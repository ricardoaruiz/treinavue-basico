-----Para executar o backend
	npm i g json-server (em qualquer pasta uma única vez).
	cd backend
	json-server --watch dados.json

-----Para executar o frontend

	cd frontend
	npm i
	npm run serve


